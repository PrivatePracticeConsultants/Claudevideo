# Every pin below ships a ready-made Windows wheel for CPython 3.11-3.14,
# so the app installs on the Python a user already has. duckdb was the one
# holdout (1.3.2 stopped at 3.13) and is why 3.12 used to be required.
httpx==0.28.1
ijson==3.5.1
duckdb==1.5.5
pyarrow==25.0.1
pydantic==2.13.4
PyYAML==6.0.3
rich==14.0.0
tqdm==4.67.1
pytest==9.1.1
fastapi==0.141.1
# pinned explicitly: transitive via fastapi, and the web layer's CVEs land here
starlette==1.6.0
uvicorn==0.35.0
watchfiles==1.2.0
python-multipart==0.0.32
cryptography==50.0.0
# optional — enables automatic crawling of JavaScript-only payer portals
# (after installing, run once: playwright install chromium)
playwright==1.61.0
